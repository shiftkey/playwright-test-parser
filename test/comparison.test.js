describe('comparison', () => {
  it.todo('returns 0 when tests take same time')
  it.todo('returns -1 when first test is slower than other test')
  it.todo('returns 1 when first test is faster than other test')
})
